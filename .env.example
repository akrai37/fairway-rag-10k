# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# The ONLY required key - embeddings run locally, so no embedding key is needed.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the generation model (defaults to claude-sonnet-4-6).
# ANTHROPIC_MODEL=claude-opus-4-8
